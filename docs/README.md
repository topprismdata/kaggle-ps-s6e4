# Documentation

- `PLAN.md` — project plan and evaluation milestones.
- `competition-log.md` — dated experiment and learning record.

The benchmark notebooks and runners remain under `notebooks/` and `scripts/`.
